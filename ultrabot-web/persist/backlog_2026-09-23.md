# UltraBot Live Market Operational Backlog & Audit Log
**Date:** 2026-09-23 (Trading Session 09:15–15:30 IST)  
**Session ID:** `c676554e-d362-45f5-a5fa-3f49d2863224`  
**Execution Mode:** PAPER on Live Fyers v3 Realtime Market Data  
**Initial Capital:** ₹4,97,228.93  
**Audit Timestamp:** 18:00 IST (Post-Market EOD)  
**Audit Status:** Final EOD Operational Audit & Post-Market Action Items  

---

## 1. Executive Status & Session Snapshot
* **Engine Status:** `STOPPED` (Graceful post-market stop; Uptime: ~29,400s (~8.1h), 0 fatal crashes, 0 errors, feed HEALTHY)
* **Market Regime:** `Sideways` (India VIX ~10.32, Nifty ~23,428.50)
* **Active Strategies:** `ORB`, `MRF`, `VC`, `SIC` (17 Shadow Strategies)
* **Real Trades Executed Today:** 1 trade (100% win rate)
  - **`COALINDIA`** | Strategy: `ORB` | Direction: `SELL` | Qty: 294 @ ₹424.84 | Exit: ₹422.71 @ 10:54:08 IST | Status: `CLOSED` | Gross P&L: **+₹626.22** | Fees: ₹92.55 | Net P&L: **+₹533.67**
* **Option Snapshots Captured:** **3,333 snapshots** across BANKNIFTY (834), FINNIFTY (832), NIFTY (834), SENSEX (833) (captured before ~12:10 socket timeout)
* **Shadow ML Outcomes Tracked:** **2,391 resolved signals** (181 Wins, 1,104 Losses, 974 Expired; 13,087 feature rows with full 20 features, 99.79% coverage)
* **Capital Protection by Risk Gates:** Over **₹1,750.00+** saved by blocking losing mid-day signals in MRF, SIC, BBR, and VR.

---

## 2. Why No New Trades Were Taken After COALINDIA

1. **ORB Entry Window Closed at 10:00 IST:**
   - Opening Range Breakout (`ORB`) is strictly restricted to market opening momentum (09:15–10:00 IST). It captured the COALINDIA short at 09:38 and successfully exited with target hit at 10:54:08 IST (+₹533.67). By design, ORB enters zero trades after 10:00 AM.
2. **Gate G14 (Strategy Backtest / Historical Profit Factor Gate) Blocked MRF & SIC:**
   - `MRF` (Mean Reversion Fade) generated 584 signals, but G14 blocked all 584 because historical profit factor in Sideways regime is 0.99 (< 1.25 minimum required).
   - In shadow tracking today, MRF took 552 trades and suffered **30 wins, 301 losses, and 221 expired** (-₹700.62 loss). Gate G14 completely protected real capital from this loss!
   - `SIC` generated 239 signals, all blocked by G14 (profit factor 0.97 < 1.25 requirement). Shadow tracking confirmed 71 losses to 16 wins (-₹377.83).
3. **Gate G15 (Volume Liquidity Gate) Filtered Low-Volume Stocks:**
   - 900 signals across VOLTAS, BANDHANBNK, CROMPTON, BHEL, POLICYBZR, etc. were rejected because mid-day relative volume fell below the 1.00x baseline.
4. **Conclusion:**
   - The bot functioned exactly as designed under institutional risk rules: preventing overtrading and capital bleed during mid-day sideways chop when strategies lack statistical edge.

---

## 3. Issues & Backlog Items Identified Today

### 🚨 Issue 1: Capital Reconciliation Audit on Same-Day Session Resume
* **Location:** `backend/core/engine.py` (Session resume / capital resolution)
* **Symptom:**
  - Upon starting the engine at ~09:38 IST after morning authentication, `self.initial_capital` loaded as **₹4,97,228.93** from the existing same-day session row.
* **Audit Note & Root Cause Analysis:**
  - **Correction:** Reconciliation already runs on both resume paths on `main`. Do NOT force-anchor to ₹5,00,999.08 (which was an inverted phantom value from an earlier session state). Forcing an arbitrary manual number would inject a real capital accounting bug.
* **Action Required:**
  - The executor must re-audit the resume reconciliation logic against merged `main`. Verify that `_reconcile_boot_capital_with_ledger()` correctly checks `resolve_total_capital(config) + get_all_time_realized_net()` across both cold boot and mid-day resume without double-counting closed session P&L.

---

### 🚨 Issue 2: Analytical Greeks Fallback Enhancement (Deferred to Oct-4)
* **Location:** `backend/options/greeks.py` & `backend/options/recorder.py`
* **Symptom:**
  - Option verification telemetry reports `status: "synthetic_computed"` with static `iv = 0.15` and `tte_years = 0.02` when Fyers feed provides no broker Greeks.
* **Nuance & Impact Assessment:**
  - All 3,333 snapshot rows captured today are explicitly flagged with `iv_assumed: true` and `tte_assumed: true` in the DB/metadata provenance. The dataset stays clean and completely filterable, ensuring zero ML dataset contamination.
  - Using `VIX / 100` as a global IV baseline is flawed because VIX is NIFTY-specific and structurally wrong for BANKNIFTY, FINNIFTY, and SENSEX.
* **Post-Market Fix / Action Required (Deferred to Oct-4):**
  - Use the Newton-Raphson iterative IV solver already present in `greeks.py:180` to back out implied volatility directly from the observed option market LTP and underlying spot price.
  - Calculate exact Time-To-Expiry ($TTE$) dynamically from the contract's expiry timestamp in instrument metadata.
  - Retain `India VIX / 100` strictly as an emergency last-resort fallback for NIFTY only when solver convergence fails.

---

### 🚨 Issue 3: Missing Class Wrapper in `scripts/ub_auth_client.py`
* **Location:** `scripts/ub_auth_client.py`
* **Symptom:**
  - Monitoring callers importing `from scripts.ub_auth_client import UBAuthClient` raised `ImportError: cannot import name 'UBAuthClient'`.
* **Why It Happened:**
  - The client script was implemented with standalone module functions (`login()`, `get_token()`, `authed_json()`), but monitoring callers expected a class wrapper `UBAuthClient`.
* **Post-Market Fix / Action Required:**
  - Add a lightweight `UBAuthClient` class wrapper exposing `.authed_json()`, `.get_token()`, and `.is_healthy()`.
  - Commit both `scripts/ub_auth_client.py` and `scripts/confirm_opportunity_watcher.py`.

---

### 🚨 Issue 4: Missing Async Timeout on Synchronous Fyers SDK Calls (`_call`)
* **Location:** `backend/brokers/fyers.py` (`_call` method, line 184) & `backend/app.py` (`_supervise_option_recorder`)
* **Symptom:**
  - Option chain recorder telemetry stopped advancing polls after ~12:10 IST (`polls_count: 3333`, `last_poll_seconds_ago: >1200s`).
* **Why It Happened:**
  - `brokers/fyers.py::_call` runs synchronous Fyers SDK methods in a thread pool via `await asyncio.to_thread(fn, *args, **kwargs)`.
  - There is NO `asyncio.wait_for(..., timeout=10.0)` wrapping the thread call. When Fyers API socket/network hung at ~12:10 IST, the thread blocked indefinitely, hanging the option recorder worker task.
  - Furthermore, `_supervise_option_recorder()` in `app.py` checked only if `option_recorder._fast_task.done()`, which remained `False` while the task was blocked on the thread, creating a supervisory blind spot.
* **Post-Market Fix / Action Required (Single-Purpose Commit):**
  - In `brokers/fyers.py:184`: wrap `asyncio.to_thread` with `await asyncio.wait_for(..., timeout=10.0)`. On `asyncio.TimeoutError`, log warning and return `{"s": "error", "message": "Fyers call timed out after 10.0s"}` (matches existing error convention).
  - In `app.py`: update `_supervise_option_recorder()` to check if `last_poll_seconds_ago > 180` while `running=True` — cancel stuck task and respawn.
  - *Honest nuance:* `asyncio.wait_for` unblocks the asyncio event loop coroutine, while the underlying OS thread terminates once its socket timeout triggers (bounded leak, completely safe).
